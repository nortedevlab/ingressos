import React from 'react';

const Home: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '50px' }}>
            <h1>React + TypeScript + Laravel 12 🚀</h1>
            <p>Integração funcionando com Vite!</p>
        </div>
    );
};

export default Home;
